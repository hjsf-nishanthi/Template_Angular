:lock: ng new vs npm install

:key: ng new 
- creates a new project  
    
:key: npm install 
- checks if libraries are available in the node_modules folder, otherwise will install the dependencies.
- uses package.json to verfiy dependencies.
